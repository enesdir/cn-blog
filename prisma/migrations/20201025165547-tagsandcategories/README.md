# Migration `20201025165547-tagsandcategories`

This migration has been generated by enes esen at 10/25/2020, 7:55:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Post" ADD COLUMN "likeCount" integer   NOT NULL DEFAULT 0

CREATE TABLE "public"."Category" (
"id" SERIAL,
"name" text   NOT NULL ,
"slug" text   NOT NULL ,
"postId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Tag" (
"id" SERIAL,
"name" text   NOT NULL ,
"slug" text   NOT NULL ,
"postId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Category.name_unique" ON "public"."Category"("name")

CREATE UNIQUE INDEX "Category.slug_unique" ON "public"."Category"("slug")

CREATE UNIQUE INDEX "Tag.name_unique" ON "public"."Tag"("name")

CREATE UNIQUE INDEX "Tag.slug_unique" ON "public"."Tag"("slug")

ALTER TABLE "public"."Category" ADD FOREIGN KEY ("postId")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Tag" ADD FOREIGN KEY ("postId")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200825211104-viewcount..20201025165547-tagsandcategories
--- datamodel.dml
+++ datamodel.dml
@@ -3,49 +3,67 @@
 }
 datasource postgresql {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
-
 model User {
-  id        Int     @default(autoincrement()) @id
-  name      String
-  surname   String
-  email     String  @unique
-  password  String
-  role      Role    @default(value: USER)
-  isDeleted Boolean @default(false)
-  posts     Post[]
-  comments  Comment[]
-  createdAt DateTime  @default(now())
+  id          Int         @default(autoincrement()) @id
+  name        String
+  surname     String
+  email       String      @unique
+  password    String
+  role        Role        @default(value: USER)
+  isDeleted   Boolean     @default(false)
+  posts       Post[]
+  comments    Comment[]
+  createdAt   DateTime    @default(now())
 }
 model Post {
-  id        Int     @default(autoincrement()) @id
-  content   String?
-  published Boolean @default(false)
-  title     String
-  author    User?   @relation(fields: [authorId], references: [id])
-  authorId  Int?
-  viewCount Int      @default(0)
-  comments  Comment[]
-  createdAt DateTime  @default(now())
-  updatedAt DateTime  @updatedAt
+  id          Int         @default(autoincrement()) @id
+  content     String?
+  published   Boolean     @default(false)
+  title       String
+  author      User?       @relation(fields: [authorId], references: [id])
+  authorId    Int?
+  viewCount   Int         @default(0)
+  likeCount   Int         @default(0)
+  comments    Comment[]
+  categories  Category[]
+  tags        Tag[]
+  createdAt   DateTime    @default(now())
+  updatedAt   DateTime    @updatedAt
 }
 model Comment {
-  id        Int      @default(autoincrement()) @id
-  contain   String
-  post      Post     @relation(fields: [postId], references: [id])
-  postId    Int
-  author    User?    @relation(fields: [authorId], references: [id])
-  authorId  Int?
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+  id          Int         @default(autoincrement()) @id
+  contain     String
+  post        Post        @relation(fields: [postId], references: [id])
+  postId      Int
+  author      User?       @relation(fields: [authorId], references: [id])
+  authorId    Int?
+  createdAt   DateTime    @default(now())
+  updatedAt   DateTime    @updatedAt
 }
+model Category {
+  id          Int         @default(autoincrement()) @id
+  name        String      @unique
+  slug        String      @unique
+  post        Post        @relation(fields: [postId], references: [id])
+  postId      Int
+}
+
+model Tag {
+  id          Int         @default(autoincrement()) @id
+  name        String      @unique
+  slug        String      @unique
+  post        Post        @relation(fields: [postId], references: [id])
+  postId      Int
+}
+
 enum Role {
   SUPER_USER
   USER
   ADMIN
```


